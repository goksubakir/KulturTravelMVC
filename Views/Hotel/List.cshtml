@model List<KulturTravelMVC.Models.Hotel>
@{
    ViewBag.Title = "Otel Listesi | Kültür Travel";
    Layout = null;
    var search = ViewBag.Search as KulturTravelMVC.Models.SearchViewModel;
}
<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewBag.Title</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="@Url.Content("~/Content/style.css")" />
    <style>
        * {
            box-sizing: border-box;
        }

        body {
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            min-height: 100vh;
            margin: 0;
        }

        .hotels-container {
            max-width: 1400px;
            margin: 40px auto;
            padding: 0 20px;
            padding-bottom: 40px;
        }

        .page-header {
            text-align: center;
            margin-bottom: 50px;
            animation: fadeInDown 0.8s ease-out;
        }

        @@keyframes fadeInDown {
            from {
                opacity: 0;
                transform: translateY(-30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .page-header h2 {
            color: #0077b6;
            font-size: 2.8rem;
            font-weight: 700;
            margin-bottom: 15px;
            background: linear-gradient(135deg, #0077b6 0%, #00b4d8 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .hotel-count-badge {
            display: inline-block;
            background: linear-gradient(135deg, #ffd60a 0%, #ffc300 100%);
            color: #000;
            padding: 10px 24px;
            border-radius: 25px;
            font-weight: 600;
            font-size: 1.1rem;
            box-shadow: 0 6px 20px rgba(255, 214, 10, 0.4);
            margin-top: 10px;
        }

        .hotels-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(380px, 1fr));
            gap: 30px;
            margin-top: 40px;
        }

        .hotel-card {
            background: white;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            animation: fadeInUp 0.6s ease-out;
            animation-fill-mode: both;
        }

        @@keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .hotel-card:nth-child(1) { animation-delay: 0.1s; }
        .hotel-card:nth-child(2) { animation-delay: 0.2s; }
        .hotel-card:nth-child(3) { animation-delay: 0.3s; }
        .hotel-card:nth-child(4) { animation-delay: 0.4s; }
        .hotel-card:nth-child(5) { animation-delay: 0.5s; }
        .hotel-card:nth-child(6) { animation-delay: 0.6s; }

        .hotel-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, #0077b6, #0096c7, #00b4d8);
            transform: scaleX(0);
            transform-origin: left;
            transition: transform 0.4s ease;
        }

        .hotel-card:hover::before {
            transform: scaleX(1);
        }

        .hotel-card:hover {
            transform: translateY(-12px) scale(1.02);
            box-shadow: 0 20px 50px rgba(0, 119, 182, 0.25);
        }

        .hotel-image-wrapper {
            position: relative;
            width: 100%;
            height: 280px;
            overflow: hidden;
        }

        .hotel-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.6s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .hotel-card:hover .hotel-image {
            transform: scale(1.15);
        }

        .hotel-image-overlay {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(to bottom, transparent 0%, rgba(0, 0, 0, 0.3) 100%);
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .hotel-card:hover .hotel-image-overlay {
            opacity: 1;
        }

        .hotel-badge {
            position: absolute;
            top: 15px;
            right: 15px;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            padding: 8px 16px;
            border-radius: 20px;
            font-weight: 600;
            color: #0077b6;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            z-index: 2;
        }

        .hotel-info {
            padding: 28px;
        }

        .hotel-name {
            color: #0077b6;
            font-size: 1.6rem;
            font-weight: 700;
            margin-bottom: 12px;
            line-height: 1.3;
            transition: color 0.3s ease;
        }

        .hotel-card:hover .hotel-name {
            color: #0096c7;
        }

        .hotel-location {
            color: #666;
            margin-bottom: 18px;
            font-size: 1rem;
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .hotel-rating {
            display: flex;
            align-items: center;
            gap: 12px;
            margin-bottom: 18px;
            flex-wrap: wrap;
        }

        .stars {
            color: #ffd60a;
            font-size: 1.3rem;
            letter-spacing: 2px;
            filter: drop-shadow(0 2px 4px rgba(255, 214, 10, 0.3));
        }

        .rating-text {
            color: #666;
            font-size: 0.95rem;
            font-weight: 500;
        }

        .hotel-description {
            color: #555;
            line-height: 1.7;
            margin-bottom: 20px;
            font-size: 0.95rem;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .price-section {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-top: 20px;
            padding-top: 20px;
            border-top: 2px solid #f0f0f0;
        }

        .price {
            font-size: 2rem;
            color: #0077b6;
            font-weight: 700;
            display: flex;
            align-items: baseline;
            gap: 5px;
        }

        .price span {
            font-size: 1rem;
            color: #999;
            font-weight: 400;
        }

        .btn-details {
            background: linear-gradient(135deg, #0077b6 0%, #0096c7 100%);
            color: white;
            padding: 12px 28px;
            border: none;
            border-radius: 12px;
            text-decoration: none;
            display: inline-block;
            font-weight: 600;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            box-shadow: 0 6px 20px rgba(0, 119, 182, 0.3);
            position: relative;
            overflow: hidden;
        }

        .btn-details::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.3);
            transform: translate(-50%, -50%);
            transition: width 0.5s, height 0.5s;
        }

        .btn-details:hover::before {
            width: 300px;
            height: 300px;
        }

        .btn-details:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 30px rgba(0, 119, 182, 0.4);
        }

        .btn-details span {
            position: relative;
            z-index: 1;
        }

        .no-results {
            text-align: center;
            padding: 80px 20px;
            background: white;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            animation: fadeIn 0.8s ease-out;
        }

        @@keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        .no-results h3 {
            color: #666;
            font-size: 1.8rem;
            margin-bottom: 20px;
        }

        .no-results p {
            color: #999;
            margin-bottom: 30px;
        }

        /* Responsive */
        @@media (max-width: 768px) {
            .hotels-grid {
                grid-template-columns: 1fr;
                gap: 25px;
            }

            .page-header h2 {
                font-size: 2rem;
            }

            .hotel-card {
                margin: 0 auto;
                max-width: 100%;
            }
        }

        /* Loading shimmer effect */
        @@keyframes shimmer {
            0% {
                background-position: -1000px 0;
            }
            100% {
                background-position: 1000px 0;
            }
        }
    </style>
</head>
<body>
    <header style="background-color: #0c3563 !important; color: white !important; text-align: center; padding: 15px 0;">
        <h1>Kültür Travel & Tourism</h1>
    </header>

    <nav style="background-color: #417fae !important; display: flex; justify-content: center; gap: 30px; padding: 10px;">
        @Html.ActionLink("Anasayfa", "Index", "Home", null, new { style = "color: white !important; text-decoration: none; font-weight: 500;" })
        @Html.ActionLink("Otel Ara", "Search", "Hotel", null, new { @class = "active", style = "color: white !important; text-decoration: none; font-weight: 500;" })
        @Html.ActionLink("Hakkımızda", "About", "Home", null, new { style = "color: white !important; text-decoration: none; font-weight: 500;" })
        @Html.ActionLink("İletişim", "Contact", "Home", null, new { style = "color: white !important; text-decoration: none; font-weight: 500;" })
        @Html.ActionLink("Giriş Yap", "Login", "Auth", null, new { style = "color: white !important; text-decoration: none; font-weight: 500;" })
    </nav>

    <div class="hotels-container">
        <div class="page-header">
            <h2>Bulunan Oteller</h2>
            <div class="hotel-count-badge">@Model.Count Otel</div>
        </div>

        @if (Model.Count == 0)
        {
            <div class="no-results">
                <h3>😔 Aradığınız kriterlere uygun otel bulunamadı</h3>
                <p>Lütfen farklı kriterlerle tekrar arama yapın.</p>
                @Html.ActionLink("Yeni Arama Yap", "Search", "Hotel", null, new { @class = "btn-details", style = "margin-top: 20px;" })
            </div>
        }
        else
        {
            <div class="hotels-grid">
                @foreach (var hotel in Model)
                {
                    <div class="hotel-card">
                        <div class="hotel-image-wrapper">
                            <img src="@(hotel.Images != null && hotel.Images.FirstOrDefault() != null ? hotel.Images.FirstOrDefault() : Url.Content("~/Content/images/default-hotel.jpg"))" 
                                 alt="@hotel.Name" 
                                 class="hotel-image" />
                            <div class="hotel-image-overlay"></div>
                            <div class="hotel-badge">@hotel.StarRating ⭐</div>
                        </div>

                        <div class="hotel-info">
                            <h3 class="hotel-name">@hotel.Name</h3>
                            <p class="hotel-location">📍 @hotel.City, @hotel.Country</p>
                            
                            <div class="hotel-rating">
                                <span class="stars">@string.Concat(Enumerable.Repeat("⭐", hotel.StarRating))</span>
                                <span class="rating-text">(@hotel.AverageRating.ToString("F1") - @hotel.TotalRatings değerlendirme)</span>
                            </div>
                            
                            <p class="hotel-description">@hotel.Description</p>
                            
                            <div class="price-section">
                                <div class="price">
                                    @hotel.PricePerNight.ToString("N0") <span>₺ / gece</span>
                                </div>
                                @Html.ActionLink("Detayları Gör →", "Details", "Hotel", new { id = hotel.Id }, new { @class = "btn-details" })
                            </div>
                        </div>
                    </div>
                }
            </div>
        }
    </div>

    <footer>
        <div class="footer-links">
            @Html.ActionLink("Hakkımızda", "About", "Home") |
            @Html.ActionLink("Bize Ulaşın", "Contact", "Home") |
            <a href="#">Gizlilik Politikası</a> |
            <a href="#">Instagram</a>
        </div>
        <p>&copy; 2025 Kültür Travel Portal. Tüm Hakları Saklıdır.</p>
    </footer>

    <!-- AUTH JS (Oturum kontrolü) -->
    <script src="@Url.Content("~/Scripts/auth.js")"></script>
</body>
</html>
